#include "main.h"

/**
  * _strstr - locates substring
  * @haystack: first
  * @needle: second
  *
  * Return: pointer
  */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
