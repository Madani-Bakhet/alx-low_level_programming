#include "main.h"

/**
  * _strncpy - copy string into another
  * @dest: destination
  * @src: source
  * *@n: num
  *
  * Return: copy string
  */
char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
