#include "main.h"

/**
  * _strcat - concatinate
  * @dest: destination
  * @src: source
  *
  * Return: the concaatination of dest & src
  */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
